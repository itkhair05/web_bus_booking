# Deny direct access to core files
# Only allow includes from PHP files

<Files "*">
    Order Deny,Allow
    Deny from all
</Files>

# Allow PHP includes
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
